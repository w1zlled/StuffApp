@model StuffApp.ViewModels.Categories.CreateCategoryViewModel

@{
    ViewData["Title"] = "Добавление категории";
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Главная</a></li>
        <li class="breadcrumb-item"><a asp-controller="Categories" asp-action="Index">Категории</a></li>
        <li class="breadcrumb-item active"><a>Создание</a></li>
    </ol>
</nav>

<h3>
    <a class="btn text-dark" asp-action="Index" title="Вернуться к списку">
        <i class="fas fa-arrow-left"></i>
    </a>
    Добавление категории
</h3>

@* <h4>Категория</h4> *@
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-3">
                <label asp-for="CategoryName" class="control-label"></label>
                <input asp-for="CategoryName" class="form-control" />
                <span asp-validation-for="CategoryName" class="text-danger"></span>
            </div>
            @* <div class="form-group mb-3">
                <label asp-for="ParentCategoryId" class="control-label"></label>
                <input asp-for="ParentCategoryId" class="form-control" />
                <span asp-validation-for="ParentCategoryId" class="text-danger"></span>
            </div> *@
            <div class="mb-3">
                <label asp-for="ParentCategoryId" class="control-label"></label>
                <select asp-for="ParentCategoryId" class="form-control form-select" asp-items="ViewBag.Categories"></select>
            </div>
            @* <div class="form-group">
                <label asp-for="IdFormOfStudy" class="control-label"></label>
                <select asp-for="IdFormOfStudy" class="form-control" asp-items="ViewBag.IdFormOfStudy"></select>
            </div> *@
            <div class="form-group">
                <input type="submit" value="Добавить" class="btn btn-primary" />
            </div>
            @* ViewData["IdAnimeTitle"] *@
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
